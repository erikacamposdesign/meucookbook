<% flash.each do |msg|%>
  <%= msg %>
<% end %>

<h1><%= @recipe_type.name %></h1>
<% @recipe_type.recipes.each do |r| %>
  <h1><%= r.title %></h1>
  <ul>
    <li><%= r.recipe_type.name %></li>
    <li><%= r.cuisine.name %></li>
    <li><%= r.difficulty %></li>
    <li><%= r.cook_time %> minutos</li>
  </ul>
<% end %>
